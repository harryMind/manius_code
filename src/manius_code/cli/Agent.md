# Agent.md

## cli module

In the main module, only the configuration is loaded, the command is parsed, and then control is passed to the sub-command. All sub-commands are set in the folder 'commands'.